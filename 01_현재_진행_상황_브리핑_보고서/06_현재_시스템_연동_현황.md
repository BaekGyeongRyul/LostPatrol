# 06. 현재 시스템 연동 현황

## [완료]

```
Web ↕ Supabase
```
웹의 모든 명령(수동 8종 + patrol_start/patrol_stop)이 실제 `robot_commands`에 INSERT되고, `robot_status`/`lost_items`를 실제로 조회·갱신하는 것을 확인했습니다.

```
Robot Mock Controller ↕ Supabase ↕ Web
```
Mock Controller가 anon key로 명령을 polling하고, heartbeat를 5초마다 갱신하며, 웹에서 ONLINE/OFFLINE 표시가 정상 동작하는 것을 확인했습니다.

```
GitHub Collaboration
GitHub Pages
```
공용 Repository + Collaborator 구조, Data Contract 문서화, GitHub Actions를 통한 자동 배포(https://BaekGyeongRyul.github.io/LostPatrol/)까지 완료했습니다.

```
Storage RLS 정책
```
`lost-item-photos` 버킷에 anon INSERT/SELECT/UPDATE RLS 정책을 적용해(2026.08.25 11:43 KST), 기존 403 오류의 원인(정책 부재)을 해결했습니다. `pg_policies`에 실제 등록된 것을 확인했습니다.

## [부분 완료 / 진행 중]

```
Camera → Storage
```
RLS 정책 적용으로 403 오류 원인은 해결되었으나, `upload_capture()` 재실행 후 실제 업로드된 파일이 `storage.objects`에서 아직 확인되지 않아 업로드 결과 자체의 재검증이 필요합니다.

```
Vision → YOLO Local Inference
```
가방 1종 모델 학습까지는 완료되었고, Roboflow 서버 추론 대신 로컬 추론으로 전환하는 작업이 진행 중입니다.

## [남음]

- Vision 3종(가방/우산/물병) 모델 학습
- Vision → `lost_items` 실제 자동 등록
- Vision → Storage 이미지 업로드
- 실제 Robot(모터/센서)/Capture/Vision 최종 통합
- Line Tracking 실제 하드웨어 검증
- Ultrasonic 안전정지 실제 하드웨어 검증
- 전체 시연 리허설
